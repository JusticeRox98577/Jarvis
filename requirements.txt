PySide6>=6.6,<7.0
ollama>=0.3.0
PyYAML>=6.0
psutil>=5.9

# Voice (optional, but installed by default). If any of these fail to
# install, Jarvis still runs fine in text-only mode.
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.26
pyttsx3>=2.90

# Windows-only: powers the opt-in active-window-title awareness feature.
pywin32>=306; platform_system=="Windows"
